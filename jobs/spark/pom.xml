<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>dev.stratus</groupId>
        <artifactId>stratus-build-parent</artifactId>
        <version>1.0-SNAPSHOT</version>
        <relativePath>../../build-support/stratus-build-parent/pom.xml</relativePath>
    </parent>

    <artifactId>stratus-spark-jobs</artifactId>
    <name>Stratus Spark Jobs</name>
    <description>Platform batch jobs: ingestion, transform, materialisation, quality, and maintenance</description>

    <properties>
        <!--
          The approved Spark runtime image is built on Java 17, so these
          classes must target it. The repository default is 25, which would
          produce a jar the cluster refuses with UnsupportedClassVersionError
          only at submission time.
        -->
        <maven.compiler.release>17</maven.compiler.release>
        <spark.version>4.1.2</spark.version>
        <spark.scala.binary>2.13</spark.scala.binary>
        <iceberg.spark.runtime.version>1.11.0</iceberg.spark.runtime.version>
    </properties>

    <dependencies>
        <!--
          Provided throughout: the runtime image already carries Spark, the
          Iceberg Spark runtime, and Jackson. Packaging any of them here would
          put a second copy on the executor classpath.
        -->
        <dependency>
            <groupId>org.apache.spark</groupId>
            <artifactId>spark-sql_${spark.scala.binary}</artifactId>
            <version>${spark.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.iceberg</groupId>
            <artifactId>iceberg-spark-runtime-4.1_${spark.scala.binary}</artifactId>
            <version>${iceberg.spark.runtime.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>2.20.0</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-jdk14</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
