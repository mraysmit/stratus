<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>dev.stratus</groupId>
        <artifactId>stratus-reactor</artifactId>
        <version>1.0-SNAPSHOT</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <artifactId>stratus-bom</artifactId>
    <packaging>pom</packaging>
    <name>Stratus Dependency BOM</name>

    <properties>
        <aws.sdk.version>2.46.8</aws.sdk.version>
        <hadoop.version>3.4.3</hadoop.version>
        <iceberg.version>1.11.0</iceberg.version>
        <junit.version>6.1.0</junit.version>
        <!-- The versions the approved Spark runtime image is built from. A
             client that submits to that cluster must match it: the driver and
             the executors exchange serialised closures, and a mismatch surfaces
             at submission rather than at build time. The authoritative set is
             platform/spark/image/artifact-lock.txt. -->
        <spark.version>4.1.2</spark.version>
        <spark.scala.binary>2.13</spark.scala.binary>
        <!-- 2.x required: Iceberg brings slf4j-api 2.x, which ignores 1.7-style
             bindings and would silently rout logging to the NOP logger. -->
        <slf4j.version>2.0.17</slf4j.version>
        <snakeyaml.version>2.4</snakeyaml.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>bom</artifactId>
                <version>${aws.sdk.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Iceberg owns its Parquet and S3 SDK compatibility set through
                 these modules and the bundle; transitive versions are never
                 independently pinned (iceberg_polaris_catalog.md section 9). -->
            <!-- Shaded Hadoop client API: parquet-java requires Hadoop's
                 Configuration class on the classpath even on Iceberg's
                 hadoop-free S3 path. The api artifact carries no server
                 dependencies. -->
            <dependency>
                <groupId>org.apache.hadoop</groupId>
                <artifactId>hadoop-client-api</artifactId>
                <version>${hadoop.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.hadoop</groupId>
                <artifactId>hadoop-client-runtime</artifactId>
                <version>${hadoop.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.iceberg</groupId>
                <artifactId>iceberg-core</artifactId>
                <version>${iceberg.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.iceberg</groupId>
                <artifactId>iceberg-data</artifactId>
                <version>${iceberg.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.iceberg</groupId>
                <artifactId>iceberg-parquet</artifactId>
                <version>${iceberg.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.iceberg</groupId>
                <artifactId>iceberg-orc</artifactId>
                <version>${iceberg.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.iceberg</groupId>
                <artifactId>iceberg-aws</artifactId>
                <version>${iceberg.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.iceberg</groupId>
                <artifactId>iceberg-aws-bundle</artifactId>
                <version>${iceberg.version}</version>
            </dependency>
            <dependency>
                <groupId>dev.stratus</groupId>
                <artifactId>stratus-iceberg-aws-runtime</artifactId>
                <version>${project.version}</version>
                <classifier>runtime</classifier>
            </dependency>
            <!-- The client side of the Spark cluster: a driver that runs
                 outside the containers and submits to the standalone master.
                 Versions match platform/spark/image/artifact-lock.txt, which is
                 what the executors run; hadoop-aws is the S3A connector for the
                 raw-file path, separate from Iceberg's own S3FileIO. -->
            <dependency>
                <groupId>org.apache.spark</groupId>
                <artifactId>spark-sql_${spark.scala.binary}</artifactId>
                <version>${spark.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.iceberg</groupId>
                <artifactId>iceberg-spark-runtime-4.1_${spark.scala.binary}</artifactId>
                <version>${iceberg.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.hadoop</groupId>
                <artifactId>hadoop-aws</artifactId>
                <version>${hadoop.version}</version>
            </dependency>
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-jdk14</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
            <dependency>
                <groupId>org.yaml</groupId>
                <artifactId>snakeyaml</artifactId>
                <version>${snakeyaml.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>
