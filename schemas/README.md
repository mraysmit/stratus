# Shared Schemas

Canonical event, quality, lineage, and configuration schemas shared by multiple applications or jobs belong here. A schema owned by only one module remains with that module until it becomes a cross-module contract.
