# Environments

Environment-specific inventory and overlays belong here under `developer/`, `acceptance/`, and `production/` as they are introduced.

This directory may contain hostnames, topology selections, sizing, and references to managed secrets. It must not contain secret values or duplicate reusable product configuration from `platform/`.
