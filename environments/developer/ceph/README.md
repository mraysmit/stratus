# Developer Ceph Overlay

Record the selected developer RGW endpoint profile and references to local protected configuration here. Secret values remain in ignored local files or the approved secret system.
