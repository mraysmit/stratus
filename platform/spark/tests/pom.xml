<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>dev.stratus</groupId>
        <artifactId>stratus-build-parent</artifactId>
        <version>1.0-SNAPSHOT</version>
        <relativePath>../../../build-support/stratus-build-parent/pom.xml</relativePath>
    </parent>

    <artifactId>stratus-spark-tests</artifactId>
    <name>Stratus Spark Tests</name>
    <description>Live Spark cluster conformance and Spark harness guardrails</description>

    <properties>
        <!-- The approved Spark runtime image is built on Java 17. This module
             holds a driver that submits to it and exchanges serialised closures
             with its executors, so it must target the same release for the same
             reason jobs/spark does: a newer class file is refused at submission
             time, not at build time. -->
        <maven.compiler.release>17</maven.compiler.release>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-slf4j2-impl</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.yaml</groupId>
            <artifactId>snakeyaml</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- The driver runs here, in the test JVM, and submits to the live
             standalone master. This is the path a client of the platform
             actually uses; running spark-submit inside the master container
             exercises the cluster's own copy of Spark and nothing about
             reaching it (docs/implementation/spark_client_submission.md). -->
        <dependency>
            <groupId>org.apache.spark</groupId>
            <artifactId>spark-sql_2.13</artifactId>
            <scope>test</scope>
            <exclusions>
                <!-- The test JVM uses Spark's native Log4j2 SLF4J provider.
                     Do not also install a JUL bridge: Stratus code logs through
                     SLF4J directly and a bidirectional bridge can recurse. -->
                <exclusion>
                    <groupId>org.slf4j</groupId>
                    <artifactId>jul-to-slf4j</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>dev.stratus</groupId>
            <artifactId>stratus-iceberg-aws-runtime</artifactId>
            <classifier>runtime</classifier>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.hadoop</groupId>
            <artifactId>hadoop-aws</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- The platform jobs, run in this client's own driver. A client of the
             platform runs job code the same way: submitted to the cluster from
             its own process. Shelling out to spark-submit inside the master
             container ran the cluster's copy of Spark and proved nothing about
             the path a client takes. -->
        <dependency>
            <groupId>dev.stratus</groupId>
            <artifactId>stratus-spark-jobs</artifactId>
            <version>${project.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
