# Names deliberately removed from the repository. One lowercase token per
# line; the guardrail test fails any tracked text file that still contains
# one. When a rename retires a name, add the old name here with a comment
# naming its replacement.

# Renamed to stratus-storage-verifier (module), stratus/storage-verifier
# (image), and storage-verifier (jar/log prefix) in July 2026.
storage-contract

# Ceph Compose canonical script names use the
# <product>-<implementation>-<action>-<object> scheme. Historical entry-point
# names remain supported as compatibility wrappers and are therefore not
# retired names. ComposeClusterScriptTest verifies both interfaces.

# Renamed to connection.env in August 2026: the file is provider-owned shared
# configuration, and plain naming was preferred over "contract" jargon.
attachment-contract

# Test classes renamed *ContractTest to *ConformanceTest in August 2026: the
# suites verify conformance to Stratus requirements; "contract" implied an
# agreement that was never the mechanism. Covers every historical variant
# (CephRgwContractTest, HarnessContractTest, and so on).
contracttest
